@model List<ShinyRockForum.Models.Discussion>

@{
    ViewData["Title"] = "Home Page";
}

<p class="text-center">Popular today!</p>

@{
        foreach (var discussion in Model)
    {
        <div class="card border-secondary mb-3" style="m">
            <div class="card-header">@discussion.Title</div>
            <div class="card-body text-secondary">
                <p class="card-text">Posted at @discussion.CreateDate by SomeUserNameHere</p>
                <p class="card-text">@discussion.Content</p>
            </div>
        </div>
    }
}

